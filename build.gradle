plugins {
    id 'fabric-loom' version '1.1.0'
}

archivesBaseName = 'Doctorsid's Bosses'
version = '1.0.0'

minecraft {
    version = "1.21.1"
    yarnMappings = "intermediary"
    loader = "0.14.0"
}

modCompile "net.fabricmc:fabric-api:${project.fabric_version}"
